export const FILE_PROCESSING_PROMPT = `Extract event details from the provided file content and convert them into ICS format calendar event(s). If no time zone is specified in file content, do NOT specify a time zone in the ICS content. The time now is ${new Date().toISOString()}. Only return the ICS content, nothing else. If there are no events, return an empty string.`; // TODO: Add time zone to the prompt, after getting the time zone from the user